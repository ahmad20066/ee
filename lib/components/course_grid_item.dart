import 'package:flutter/material.dart';

class CourseGridItem extends StatelessWidget {
  const CourseGridItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
